dockworker:
  version: '3.0'
  application:
    name: composites.lib.unb.ca
    finish_marker: '99_z_notify_user_URI'
    project_prefix: COMP
    upstream_images:
      - mailhog/mailhog
      - mysql:5.7
      - redis:5-alpine
      - unblibraries/dockworker-drupal:latest
  deployment:
    environments:
      - dev
      - prod
  github:
    repo: composites.lib.unb.ca
    owner: unb-libraries
  image:
    push_branches:
      - dev
      - prod
    name: unblibraries/composites.lib.unb.ca
    date_tag_image: true
    repository: dockercloud
